---
target: app
type: added
area: customization
---

A blank `custom.css` is now created automatically and loaded after core
styles, so you can drop in your own CSS overrides without editing core
files or losing them on update.
