# omnideck app 0.2.3

## Changed

- **Artifacts:** Artifact grids now load previews only near the visible viewport and release them while the Artifacts tab is inactive. The catalog, search, sorting, and layout remain ready when returning to the tab, reducing background CPU and memory use without removing live previews.
