# omnideck app 0.2.4

## Fixed

- **Browser:** When a dialog or full-screen overlay blocks a page, browser tools now show only the controls the agent can actually use instead of covered background controls that clicks cannot reach.
