{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hosted-desktop",
  "description": "The loopback application may open validated external links and use typed desktop updates.",
  "windows": ["hosted-app"],
  "remote": {
    "urls": ["http://127.0.0.1:*"]
  },
  "permissions": ["hosted-desktop", "core:webview:allow-set-webview-zoom"]
}
