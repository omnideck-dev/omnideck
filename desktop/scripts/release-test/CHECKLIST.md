# Manual release check

The authoritative manual procedures now live in
[`desktop/tests/manual`](../../tests/manual/README.md). Start there and use the
scripts in this directory only as scenario launchers.

This compatibility file remains so older release notes and tester bookmarks do
not lead to a missing path.
